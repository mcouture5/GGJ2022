import { Scene, Game } from 'phaser';

export class GameScene extends Phaser.Scene {
   
    constructor() {
        super({
            key: 'GameScene'
        });
    }

    init(config): void {
        
    }

    create(): void {
        
    }

    update(): void {
        
    }
}
